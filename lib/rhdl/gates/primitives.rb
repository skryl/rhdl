# Gate primitives for gate-level simulation

module RHDL
  module Gates
    module Primitives
      AND = :and
      OR = :or
      XOR = :xor
      NOT = :not
      MUX = :mux
      BUF = :buf
      CONST = :const
    end
  end
end
