============================================================
Component: popcount
Type: PopCount
============================================================

═══ popcount (PopCount) ═══

Interface:
     ┌────────────────────┐
     │      popcount      │
     │     (PopCount)     │
     ├────────────────────┤
────▶├a[8]       count[4] ┤▶────
     └────────────────────┘


Hierarchy:
----------------------------------------
popcount [PopCount] (1 in, 1 out)