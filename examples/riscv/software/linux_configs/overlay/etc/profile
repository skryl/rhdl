#!/bin/sh

# Default shell environment for RHDL Linux initramfs.
# Disable ANSI color escapes in UART terminals when ls supports --color.
if /bin/busybox ls --color=never / >/dev/null 2>&1; then
  alias ls='ls --color=never'
fi
